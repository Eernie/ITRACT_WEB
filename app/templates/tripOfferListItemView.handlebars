<h4 class="pull-left">Trip offer #{{tripOffer.id}}</h4>
<p>
    <table class="table table-striped table-hover table-bordered">
        <tr>
            <td>Driver</td><td>
                <a class="user-popover" rel="popover" data-placement="right" data-content="</div><div class='pull-right'><img src='{{tripOffer.user.profilePicture}}'></img></div><div><p>Name: {{tripOffer.user.firstName}} {{tripOffer.user.lastName}}</p><p>Date of birth: {{tripOffer.user.dateOfBirth}}</p><p>Gender: {{tripOffer.user.gender}}</p><p>Rating: 4</p>" data-original-title="User">
                    {{tripOffer.user.firstName}} {{tripOffer.user.lastName}}
                </a>
            </td>
        </tr>
        <tr>
            <td>From</td>
            <td>{{tripOffer.originAddress}} <span class="badge badge-important">A</span></p></td>
        </tr>
        <tr>
            <td>To</td>
            <td>{{tripOffer.destinationAddress}} <span class="badge badge-important">B</span></p></td>
        </tr>
        <tr>
            <td>Departure time</td>
            <td>{{dateFormatTimeOnly tripOffer.startTimeMin}}</td>
        </tr>
        <tr>
            <td>Arrival time</td>
            <td>{{dateFormatTimeOnly tripOffer.endTimeMin}}</td>
        </tr>
    </table>
</p>
<img src="http://maps.googleapis.com/maps/api/staticmap?size=600x300&maptype=roadmap&markers=color:Red%7Clabel:A%7C{{{tripOffer.originLat}}, {{tripOffer.originLong}}&markers=color:Red%7Clabel:B%7C{{tripOffer.destinationLat}}, {{tripOffer.destinationLong}}&sensor=false"></img>