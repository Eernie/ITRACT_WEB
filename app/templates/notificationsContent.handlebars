{{#each notifications}}
    <li><a href="#notification-{{id}}" class="notification-list-item" data-id="{{id}}" id="modal-{{id}}" class="notificationLink" data-toggle="modal">{{id}}</a></li>
{{/each}}
