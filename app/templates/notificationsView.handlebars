 <ul class="nav">
    <li id="fat-menu" class="dropdown">
      <a href="#" id="notifications-dropdown" role="button" class="dropdown-toggle" data-toggle="dropdown">Notifications <span class="badge badge-important" id="notification-count">{{notifications.length}}</span></a>
      <ul class="dropdown-menu" role="menu" aria-labelledby="notifications-dropdown">
        <div id="notifications-content">
        </div>
      </ul>
    </li>
</ul>
